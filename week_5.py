import os
import shelve


string_dictionary = {
    'dictionaries': 'are different',
    'than': 'lists',
    'dicts': 'are key-value pairs'
}
print('String dictionary keys: and values.')
print(string_dictionary)
print('\nString dictionary type (data structure)')
print(type(string_dictionary))
